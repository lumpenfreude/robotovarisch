from nio import MatrixRooms

class RoomCommands(object):
    def __init__(self, client, store, config, content, room, event):
        self.client = self
        self.store = store
        self.config = config
        self.content = content
        self.room = room
        self.event = event


